# Credentials — register a free sandbox account at https://remarkets.primary.ventures/
PRIMARY_USER=
PRIMARY_PASS=

# Environment: leave empty or set to "remarket" for sandbox; set to "live" for production
PRIMARY_ENV=

# Required only when PRIMARY_ENV=live (URL depends on your broker, e.g. Eco Valores)
PRIMARY_BASE_URL=
PRIMARY_WS_URL=
